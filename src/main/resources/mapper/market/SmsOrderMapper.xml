<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youlai.boot.market.order.mapper.SmsOrderMapper">

    <select id="getSmsOrderPage" resultType="com.youlai.boot.market.order.model.vo.SmsOrderPageVO">
        SELECT
            id,
            order_no,
            country_name,
            has_area_code,
            scheduled_time,
            status,
            CASE status
                WHEN 0 THEN '待发送'
                WHEN 1 THEN '发送中'
                WHEN 2 THEN '已完成'
                WHEN 3 THEN '发送失败'
                WHEN 4 THEN '已取消'
                ELSE '未知'
            END AS status_desc,
            success_count,
            fail_count,
            total_count,
            create_time
        FROM sms_order
        <where>
            <if test="queryParams.orderNo != null and queryParams.orderNo != ''">
                AND order_no LIKE CONCAT('%', #{queryParams.orderNo}, '%')
            </if>
            <if test="queryParams.countryId != null">
                AND country_id = #{queryParams.countryId}
            </if>
            <if test="queryParams.status != null">
                AND status = #{queryParams.status}
            </if>
            <!-- 数据权限：非管理员只能查看自己的订单 -->
            <if test="!isRoot">
                AND create_by = #{currentUserId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
